Huge improvement in survey questions, feels as though the questions will actually be useful for the reccomendations to be helpful. Also splitting the registration and survey into two seperate pages makes it feel much more user-friendly, as it does not overwhelm the user as much as it did before.

What is working 

1) The survey questions are much more user-friendly and actually make sense
2) The survey is split into two pages, making it less overwhelming 
3) The data seems to be stored in the heating_data.db in a sensible way 

 There are a few bugs and changes that i would like to make left: 

1) The time of use time ranges are not inputted correctly, when the user clicks the range nothing happens. 
2)Weird bug where if the user selects 'yes', and then single rate for example, if they then change their mind and select 'no', the single rate questions remain. Same happen for the time of use questions. 
3) When the user registers and logs in, it shows 'No data found' This isnt a bug, as this is the expevted behaviour, but this would not be ideal to show to markers, as it feels very unprofessional. So instead I think i will give the markers demo users to log in to, and will also make it so that they can view the survey from the home page. 
4) I might need to remove all the questions that require typing, as the screen that this will be on may not support it. 
