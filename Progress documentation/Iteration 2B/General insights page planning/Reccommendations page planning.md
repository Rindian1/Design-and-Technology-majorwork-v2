Currently, the general insights page does not contain data to demosntrates the users growth over time, which is something that should be included.

1) An all time chart, which shows the users energy usage over the entire time they have been using the app. This chart should change in its scaling of the x-axis depending on how much data is on the users profile. If the user has been using it for a short period of time, each point of the chart should be the average energy usage every two days. If the user has been using it for months, it should be the average energy usage every week. The y-axis should be the energy spendage in dollars. 
This chart should be placed at the bottom of the general insights page.
This chart is going to be a line chart.

2) The current chart showing the users energy usage has the y axis in kw/h, this should be changed to dollars. 

3) Make sure all text is accesible for people of all ages. Remember that this is beign displayed on a small screen, so it is very important for the text to be clear and easy to read. This means, when possible there should be minimal text crammed in small text boxes. 